---
import { getEntry } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { getEntries } from "astro:content";

const info = await getCollection("movieInfo");
---

<BaseLayout title="MovieStore-lab">
  <main class="container">
    <article>
      
        {info.map((m) => <h1>{m.data.original_title}</h1>
        
        <p>{m.data.overview}</p>
        <footer><a href={m.data.homepage}>{m.data.homepage}</a></footer>
        )}
      
    </article>
  </main>
  <blockquote>
    Contact is a funny word. Some people think design means how it looks. But of course, if you dig deeper, it's really
    how it works.”
    <footer>
      <cite>— Steve Jobs</cite>
    </footer>
  </blockquote>
  
</BaseLayout>
<style>
    a:hover {
        color: var(--pico-primary-focus);
    }
</style>

